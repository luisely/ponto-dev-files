{
  "name": "ponto",
  "version": "1.0.0",
  "main": "main.ts",
  "scripts": {
    "clean": "rimraf dist",
    "build:html": "cpy index.html dist",
    "build:js": "esbuild src/main.ts --bundle --minify --outfile=dist/bundle.js",
    "build:css": "npx @tailwindcss/cli -i ./css/main.css -o ./dist/output.css --minify",
    "build:prod": "npm run build:js && npm run build:css && npm run build:html",
    "dev:html": "cpy index.html dev --watch",
    "dev:js": "esbuild src/main.ts --bundle --sourcemap --outfile=dev/bundle.js --watch",
    "dev:css": "npx @tailwindcss/cli -i ./css/main.css -o ./dev/output.css --watch",
    "dev": "concurrently \"npm run dev:js\" \"npm run dev:css\" \"npm run dev:html\"",
    "deploy": "npm run build:prod && cd dist && git add . && git commit -m \"Deploy\" && git push"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/toastify-js": "^1.12.4",
    "@types/vanillajs-datepicker": "^1.3.5",
    "axios": "^1.13.2",
    "tailwindcss": "^4.1.13",
    "toastify-js": "^1.12.0",
    "vanillajs-datepicker": "^1.3.4"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.11",
    "@tailwindcss/cli": "^4.1.18",
    "concurrently": "^9.2.1",
    "cpy-cli": "^6.0.0",
    "esbuild": "^0.27.2",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  }
}
